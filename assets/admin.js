/*
 * CMS Simple — Admin entry point
 */

// Admin styles (Bootstrap + Icons + Admin layout)
import './styles/admin.scss';

// Bootstrap JS
import * as bootstrap from 'bootstrap';
window.bootstrap = bootstrap;
